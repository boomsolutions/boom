@model MvcApplication4.Models.ComentarioAlquileres

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>ComentarioAlquileres</legend>

        @Html.HiddenFor(model => HttpUtility.UrlEncode(MvcApplication4.Controllers.Funciones.Encrypt(model.IdComentarioAlquiler.ToString())))

        <div class="editor-label">
            @Html.LabelFor(model => model.IdAlquiler, "Alquiler")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IdAlquiler", String.Empty)
            @Html.ValidationMessageFor(model => model.IdAlquiler)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IdUsuario, "Usuario")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IdUsuario", String.Empty)
            @Html.ValidationMessageFor(model => model.IdUsuario)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Comentario)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Comentario)
            @Html.ValidationMessageFor(model => model.Comentario)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
