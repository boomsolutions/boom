@model IEnumerable<MvcApplication4.Models.FotoProductos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Producto
        </th>
        <th>
            RutaFoto
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RutaFoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFotoProducto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFotoProducto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFotoProducto })
        </td>
    </tr>
}

</table>
