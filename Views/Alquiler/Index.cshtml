@model IEnumerable<MvcApplication4.Models.Alquileres>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Producto
        </th>
        <th>
            FechaDesde
        </th>
        <th>
            FechaHasta
        </th>
        <th>
            Usuario Arrendatario
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaDesde)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaHasta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioArrendatario.User)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAlquiler }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAlquiler }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAlquiler })
        </td>
    </tr>
}

</table>
